name: Build and deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

env:
  NODE_VERSION: 24
  REGISTRY: ghcr.io
  IMAGE_NAME: object-object/hexxy-weblate

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit

  push-image:
    needs:
      - build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    environment:
      name: docker
      url: https://${{ needs.build.outputs.image-tag }}
    outputs:
      digest: ${{ steps.digest.outputs.value }}
    steps:
      - uses: docker/setup-buildx-action@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Download image artifact
        uses: actions/download-artifact@v8
        with:
          name: image.tar
          path: ${{ runner.temp }}

      - name: Load image
        run: docker load --input ${{ runner.temp }}/image.tar

      - name: Push image
        run: docker image push --all-tags ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}

      - name: Get image digest
        id: digest
        run: echo "value=$(docker inspect --format='{{index .RepoDigests 0}}' ${{ needs.build.outputs.image-tag }})" >> "$GITHUB_OUTPUT"

  deploy-aws-cdk:
    needs:
      - build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: aws-cdk
    concurrency:
      group: deploy-aws-cdk
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7

      - uses: object-Object/ci/setup@v0
        with:
          node-version: ${{ env.NODE_VERSION }}
          npm-packages: aws-cdk
          role-to-assume: repo

      - name: Download synth artifact
        uses: actions/download-artifact@v8
        with:
          name: aws-cdk-synth
          path: cdk.out

      - name: Deploy stack
        run: cdk deploy --ci --no-lookups --require-approval=never --app=cdk.out --outputs-file=aws-cdk-outputs.json

      - name: Upload outputs artifact
        uses: actions/upload-artifact@v7
        with:
          path: aws-cdk-outputs.json
          archive: false

  deploy-codedeploy:
    needs:
      - build
      - push-image
      - deploy-aws-cdk
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    environment:
      name: codedeploy
      url: ${{ steps.deploy.outputs.deployment-url }}
    concurrency:
      group: deploy-codedeploy
      cancel-in-progress: false
    steps:
      - uses: actions/checkout@v7

      - name: Download outputs artifact
        uses: actions/download-artifact@v8
        with:
          name: aws-cdk-outputs.json

      - name: Parse outputs
        id: parse
        uses: object-Object/ci/parse-cdk-outputs@v0
        with:
          file: aws-cdk-outputs.json

      - uses: object-Object/ci/setup@v0
        with:
          role-to-assume: ${{ steps.parse.outputs.ActionsCodeDeployRoleARN }}

      - name: Create runtime files
        working-directory: codedeploy
        run: |
          cat <<END_OF_FILE > .env
          IMAGE="${{ needs.push-image.outputs.digest }}"
          WEBLATE_EMAIL_HOST=${{ secrets.WEBLATE_EMAIL_HOST }}
          WEBLATE_EMAIL_PORT=${{ vars.WEBLATE_EMAIL_PORT }}
          WEBLATE_EMAIL_HOST_USER=${{ secrets.WEBLATE_EMAIL_HOST_USER }}
          WEBLATE_EMAIL_HOST_PASSWORD=${{ secrets.WEBLATE_EMAIL_HOST_PASSWORD }}
          END_OF_FILE

      - name: Deploy application
        id: deploy
        uses: object-Object/ci/deploy-codedeploy@v0
        with:
          path: codedeploy
          stack: ${{ steps.parse.outputs.stack-name }}
          application: ${{ steps.parse.outputs.ApplicationName }}
          deployment-group: ${{ steps.parse.outputs.DeploymentGroupName }}
          s3-bucket: ${{ steps.parse.outputs.ArtifactsBucketName }}
