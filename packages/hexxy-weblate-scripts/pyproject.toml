[build-system]
requires = ["uv_build>=0.12.1,<0.13"]
build-backend = "uv_build"

[project]
name = "hexxy-weblate-scripts"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "typer>=0.27.1",
    "weblate-language-data",
]
