[build-system]
requires = ["uv_build>=0.12.1,<0.13"]
build-backend = "uv_build"

[project]
name = "hexxy-weblate"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "json5>=0.15.0",
    # Keep in sync with Dockerfile
    "hexdoc~=2.0rc1",
    # Keep in sync with Dockerfile
    "weblate~=2026.8.1",
]
