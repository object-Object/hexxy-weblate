[build-system]
requires = ["uv_build>=0.12.1,<0.13"]
build-backend = "uv_build"

[project]
name = "hexxy-weblate-infrastructure"
version = "0.0.0"
requires-python = ">=3.14"
dependencies = [
    "aws-cdk-github-oidc>=2.4.1",
    "aws-cdk-lib>=2.180.0",
    "object-ci[aws-cdk] @ git+https://github.com/object-Object/ci@v0",
]
